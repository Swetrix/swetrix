import { createSlice, PayloadAction } from '@reduxjs/toolkit'

// alerts: [],
// total: 0,
// pageTotal: 0,
// loading: false,

interface IInitialState {
    alerts: any[]
    total: number
    pageTotal: number
    loading: boolean
}
